const el = document.getElementById('app')!;
el.innerHTML = `
  <main style="font-family: ui-sans-serif, system-ui; padding: 2rem;">
    <h1>Weed Breed Client (/app)</h1>
    <p>If you see this, Vite is serving from <code>/app</code>.</p>
  </main>
`;
